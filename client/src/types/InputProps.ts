export type InputProps = {
    isEnabled?: boolean,
    name: string,
    label: string,
    icon: string,
    values?: any,
    callback?: any,
    register?: any,
    emptyOn?: any,
    doubleWidth?: boolean
}
